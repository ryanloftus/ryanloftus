import React from "react";

import { Subheading } from "./Subheading";

// currently not being used, once I have more publications, I can add them here and start using this section
// for now writing will be in the projects section
export const WritingSection: React.FC<{}> = () => (
  <>
    <Subheading text="Writing" />
  </>
);
